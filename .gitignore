# --- Never publish these ---

# The paid product zips (bot delivers them privately)
dist/

# Bot token / secrets
bot/config.json
bot/.venv/
bot/__pycache__/
*.pyc

# Local tooling / skills (not part of the store)
.claude/
.agents/
.cursor/
.gemini/
node_modules/

# Marketing kit — kept local, not part of the published storefront
marketing/

# Safe to publish: index.html + templates/  (storefront + live previews)
